<Window x:Class="Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf"
        xmlns:vm="clr-namespace:Wpf.ViewModels"
        xmlns:conv="clr-namespace:Wpf.Converters"
        mc:Ignorable="d"
        Title="Todo List" Height="600" Width="1000" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Styl przycisków -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.95" />
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#AA999999" BlurRadius="10" ShadowDepth="0" Opacity="0.6" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl nagłówków kolumn -->
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0"
                                SnapsToDevicePixels="True">
                            <ContentPresenter RecognizesAccessKey="False" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.95" />
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#AA999999" BlurRadius="10" ShadowDepth="0" Opacity="0.6" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Focusable" Value="False" />
            <Setter Property="IsTabStop" Value="False" />
        </Style>

        <!-- Styl pola daty wewnątrz DatePicker -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="4,2" />
        </Style>

        <!-- Styl elementów listy (lista zadań) -->
        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,2" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                BorderBrush="LightGray"
                                BorderThickness="1"
                                CornerRadius="0"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True"
                                Padding="4">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Podświetlenie zielone, gdy zadanie skończone -->
                            <DataTrigger Binding="{Binding IsDone}" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E8F7E8" />
                            </DataTrigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.98" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Konwerter widoczności placeholdera -->
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="EmptyToVisibility" />
    </Window.Resources>

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Góra: przycisk dodawania i kalendarz -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right" Width="48" Height="48" Margin="8,0,0,0"
                        Command="{Binding AddItemCommand}"
                        Background="#2E8B57" Foreground="White" FontSize="26" FontWeight="Bold"
                        Content="+" HorizontalAlignment="Right" VerticalContentAlignment="Center" Padding="0" />
            </DockPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Width="36" Height="30" Command="{Binding ChangeDateCommand}" 
                        CommandParameter="{Binding SelectedDate, Converter={StaticResource DateShiftConverter}, ConverterParameter=-1}">
                    <TextBlock Text="‹" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center" />
                </Button>

                <DatePicker SelectedDate="{Binding SelectedDate}" Margin="8,0" Width="122" />

                <Button Width="36" Height="30" Command="{Binding ChangeDateCommand}"
                        CommandParameter="{Binding SelectedDate, Converter={StaticResource DateShiftConverter}, ConverterParameter=1}">
                    <TextBlock Text="›" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- Nagłówki kolumn i lista zadań -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Style="{StaticResource HeaderButtonStyle}"
                        Command="{Binding SetSortFieldCommand}" CommandParameter="IsDone"
                        Content="Status" Focusable="False" IsTabStop="False" MinWidth="60" />

                <Button Grid.Column="1" Style="{StaticResource HeaderButtonStyle}"
                        Command="{Binding SetSortFieldCommand}" CommandParameter="Title"
                        Content="Opis zadania" Focusable="False" IsTabStop="False" />

                <Border Grid.Column="2" Background="Transparent" />
            </Grid>

            <ListView x:Name="ItemsList" Grid.Row="1" ItemsSource="{Binding Items}" HorizontalContentAlignment="Stretch" Padding="0,0,2,0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Focusable" Value="False" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="0" Margin="15,0,8,0" Width="20" Height="20" FontSize="14" />

                            <Grid Grid.Column="1" Margin="20,0,0,0">
                                <TextBox x:Name="TitleBox" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" BorderThickness="0" Background="Transparent" Margin="0" />
                                <TextBlock Text="Nowe zadanie..." IsHitTestVisible="False" Foreground="Gray" VerticalAlignment="Center" Margin="4,0,0,0" Visibility="{Binding Title, Converter={StaticResource EmptyToVisibility}}" />
                            </Grid>
                            <Button Grid.Column="2" Width="34" Height="28" Margin="16,0,0,0" HorizontalAlignment="Right"
 Click="DeleteButton_Click"
                                    Background="Red" Foreground="White" FontWeight="Bold" Content="✖" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

    </Grid>
</Window>
